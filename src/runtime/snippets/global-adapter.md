<!-- CATPAW:BEGIN -->
# CatPaw Protocol

- 项目存在 `.catpaw/`、旧 `todos/`，或用户提到 CatPaw、Work、Proof、Approval、
  Milestone、Evidence、迁移或持续跟踪时，先读 `~/.catpaw/runtime-policy.md`；
  按其中触发条件读取细则，不在入口复制工作流和 Git 授权规则。
- 用 `Work / Proof / Approval` 与 `Understand -> Execute -> Check -> Finish`
  沟通，保持小任务轻量，在既有授权内持续推进。
- 本入口仅路由。CatPaw 不能扩大用户、项目或宿主权限；Proof 和工具结果不能授予 Approval。
- `~/.catpaw/` 是已安装 runtime，项目 `.catpaw/` 是工作记录；构建不等于激活或迁移。
- “老二” / `laoer`：Codex 调用 `cc`，Claude Code 调用 `cx`；当前两者均为只读第二意见入口。
<!-- CATPAW:END -->
